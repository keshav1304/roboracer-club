# Playground gateway dependencies (installed on top of the Devkit image,
# which already provides rclpy + the pinned Socket.IO stack for the bridge).
#
# NOTE: fastapi/uvicorn are modern and coexist with the old Flask-SocketIO
# pins because the bridge and the gateway are separate processes.
fastapi==0.110.0
uvicorn==0.29.0
websockets==12.0

# Mapping: raceline optimization + map processing.
# Versions chosen for compatibility with the pinned numpy==1.22.2
# (scikit-image 0.19.x is the last line supporting numpy 1.22).
# PyYAML is NOT pinned here: the ROS base image ships it via apt and
# pip cannot cleanly replace a distutils-installed package.
cma==3.3.0
scipy==1.10.1
scikit-image==0.19.3

# Mapping kinematic MPC (cvxpy → OSQP). Pins chosen for numpy==1.22.x.
cvxpy==1.3.2
osqp==0.6.3
ecos==2.0.12
scs==3.2.3
# setuptools≥70 calls packaging.canonicalize_version(strip_trailing_zero=…);
# older packaging in the ROS image lacks that kwarg and breaks colcon build.
packaging>=24.0
